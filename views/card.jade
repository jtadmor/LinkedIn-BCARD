extends layout

block content
	//- Load all content via card.jsx

block scripts
	//- Script to have access to the user's id and a variable passwordEntered that determines whether fields can be edited or not
	script
		|var user_id = "#{id}";
		|var passwordEntered = false;
	script(src="/card.js" )
	
	
